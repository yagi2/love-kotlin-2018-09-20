## TextView
<br />
次に`textView`を同じように作っていく
  
```Kotlin
fun textView(init: () -> Unit): TextView =
    TextView(context)
```
<br />  
これでここまで書けるようになった  
```Kotlin
linearLayout {
    textView {
    }
} 
```   