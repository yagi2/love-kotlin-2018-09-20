## プロパティのセット
<br />
`setColor()`,`setMargins()` を  
`color = ...`, `margin =...` で書きたい  
<br />
理想の完成形
<br />
```Kotlin
linearLayout {
    textView {
        text = "Hello,Kotlin DSL!"
        color = Color.RED
    }.lparams {
        margin = dip(10)
    }
}
```