## 9割完成
<br />
今までの実装で以下のように書いて表示することができるようになった
<br />
```Kotlin
linearLayout {
    textView {
        text = "Hello,Kotlin DSL!"
        setColor(Color.RED)
    }.lparams {
        setMargins(dip(10))
    }
}
```
<br />  
`setColor()`,`setMargins()`……？🤔